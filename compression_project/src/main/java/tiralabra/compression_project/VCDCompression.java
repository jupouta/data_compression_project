package tiralabra.compression_project;


public class VCDCompression {
    
    
    public void compress(int b, String text) {
        
        // initialize fp
        for (int i = b; i < text.length(); i++) {
            if (i % b == 0) {
                // store(fp, i);
            }
            
            
        }
    }
}
